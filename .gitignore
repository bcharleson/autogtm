# AutoGTM ships the framework, never a company's data.
# A real deployment's brain and campaign logs are PRIVATE — keep them out of any public fork.

# Secrets
.env
.env.*
*.key
config.json
config.local.yaml

# Live brain content. Folder structure + .gitkeep stay tracked.
brain/**/*.md
!brain/README.md

# OS / tooling
.DS_Store
node_modules/
__pycache__/
*.pyc
.venv/
.claude/
.cursor/
.grok/
